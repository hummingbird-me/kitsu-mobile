import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flexGrow: 1,
  },
  header: {
    height: 80,
    backgroundColor: 'red',
  },
  carousel: {
    flexGrow: 1,
  },
});

export default styles;
