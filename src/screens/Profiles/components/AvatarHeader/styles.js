import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrap: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  main: {
    flex: 1,
    marginLeft: 10,
  },
  side: {
    alignSelf: 'flex-start',
  },
});
