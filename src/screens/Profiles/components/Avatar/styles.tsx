import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrap: {
    width: 30,
    height: 30,
    borderRadius: 30,
    position: 'relative',
    overflow: 'hidden',
    backgroundColor: '#F1F1F1',
  },
});
