import glamorous from 'glamorous-native';
import { ProgressiveImage } from 'kitsu/components/ProgressiveImage';

const StyledProgressiveImage = glamorous(ProgressiveImage)({
  width: '100%',
  height: '100%',
});

export default StyledProgressiveImage;
