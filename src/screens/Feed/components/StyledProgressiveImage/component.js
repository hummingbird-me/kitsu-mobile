import React from 'react';
import { ProgressiveImage } from 'kitsu/components/ProgressiveImage';

export const StyledProgressiveImage = props => (
  <ProgressiveImage style={{ width: '100%', height: '100%' }} {...props} />
);
