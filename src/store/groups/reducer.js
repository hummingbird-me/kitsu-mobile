import * as types from 'kitsu/store/types';

const INITIAL_STATE = {
  groupMemberships: [],
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
